#!/usr/bin/env python3
import os
import sys
from subprocess import run

run(
    ["docker-compose", "run", "-p", "8069:8069", "odoo", "odoo"] + sys.argv[1:],
    env=dict(os.environ, USER_ID=str(os.getuid())),
    cwd=os.path.dirname(os.path.realpath(__file__)),
)
